# List of commands used on Course

```bash
# Create new project
composer create-project --prefer-dist laravel/laravel marketplace "6.*"

# Create migration
php artisan make:migration create_store_table
```
